# Copy to .env for local dev. The admin SPA talks to the same pc/v1
# namespace as the player SPA — only admin-gated endpoints under
# pc/v1/admin/* are used here.
VITE_API_BASE_URL=https://pusher-coin.ddev.site/wp-json/pc/v1
